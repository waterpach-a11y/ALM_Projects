rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projectDocuments/{projectId}/{allPaths=**} {
      allow read, write: if request.auth != null; // à raffiner avec rôles projet
    }
  }
}
