{
  "name": "alm-functions",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions,firestore,auth,hosting",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "config:set": "node scripts/set-env.js"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "private": true,
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
